# Task 1 Review Rubric

Review Task 1 only against `.superpowers/sdd/task-1-brief.md` and its report.

Required verdicts:

1. **Spec compliance:** Does `landing-settings.ts` expose the exact four interfaces, enforce bounds, reject unsafe URL schemes, preserve secure defaults, strip administrative fields from public output, and sanitize WhatsApp to digits?
2. **Code quality:** Are Zod defaults, fallback rules and tests clear and minimal? Is TypeScript strict without `any` escape hatches? Are existing platform flows unchanged except for the new reusable contract?

Do not review publication/versioning, admin UI, marketing UI or image assets. List Critical, Important and Minor findings separately. End with both required verdicts: `Spec: approved/rejected`; `Quality: approved/rejected`.
