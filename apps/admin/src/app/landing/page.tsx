"use client";
import Link from "next/link"; import { FormEvent,useEffect,useState } from "react";
const api=process.env.NEXT_PUBLIC_API_URL??"https://api.useorien.com.br/api/v1";async function call(path:string,init:RequestInit={}){const r=await fetch(`${api}${path}`,{...init,credentials:"include",headers:{"content-type":"application/json",...(init.headers??{})}});const b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.message??"Não foi possível salvar.");return b}
export default function Landing(){const [settings,setSettings]=useState<any>({heroCta:"Começar agora",supportEmail:"suporte@useorien.com.br",showCalculator:true,showTestimonials:false}),[message,setMessage]=useState("");useEffect(()=>{void call("/platform/landing").then(setSettings).catch(e=>setMessage(e.message))},[]);async function save(e:FormEvent){e.preventDefault();try{setSettings(await call("/platform/landing",{method:"PATCH",body:JSON.stringify(settings)}));setMessage("Configurações salvas.")}catch(e){setMessage(e instanceof Error?e.message:"Erro")}}return <main className="main"><Link className="text-button" href="/">← Central</Link><p className="eyebrow">MARKETING</p><h1>Configuração da landing</h1><form className="panel" onSubmit={save}><label>CTA principal<input value={settings.heroCta??""} onChange={e=>setSettings({...settings,heroCta:e.target.value})}/></label><label>E-mail comercial<input value={settings.supportEmail??""} onChange={e=>setSettings({...settings,supportEmail:e.target.value})}/></label><label className="check"><input type="checkbox" checked={!!settings.showCalculator} onChange={e=>setSettings({...settings,showCalculator:e.target.checked})}/><span><strong>Exibir calculadora</strong></span></label><label className="check"><input type="checkbox" checked={!!settings.showTestimonials} onChange={e=>setSettings({...settings,showTestimonials:e.target.checked})}/><span><strong>Exibir depoimentos</strong></span></label><button className="btn primary">Salvar landing</button>{message&&<p className="feedback success">{message}</p>}</form></main>}
